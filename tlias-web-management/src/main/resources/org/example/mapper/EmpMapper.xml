<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.EmpMapper">

    <!--    <select id="findAll" resultType="org.example.pojo.Emp">-->
<!--        select emp.*, dept.name from emp left join dept on emp.dept_id = dept.id limit 0,5-->
<!--    </select>-->

    <select id="list" resultType="org.example.pojo.Emp">
        select emp.*, dept.name deptName
        from emp left join dept on emp.dept_id = dept.id
        <where> <!-- 自动生成where关键字，自动去除多余的and或者or-->
            <if test="name != null and name != '' ">  <!-- 动态SQL-->
                emp.name like concat('%',#{name},'%')
            </if>
            <if test="gender != null">
                and gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and entry_date between #{begin} and #{end}
            </if>
        </where>
        order by emp.update_time desc
    </select>


    <delete id="deleteById">
        delete from emp where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

   <!-- 定义resultMap   -->
    <resultMap id="empResultMap" type="org.example.pojo.Emp">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="name" column="name"/>
        <result property="gender" column="gender"/>
        <result property="phone" column="phone"/>
        <result property="job" column="job"/>
        <result property="salary" column="salary"/>
        <result property="image" column="image"/>
        <result property="entryDate" column="entry_date"/>
        <result property="deptId" column="dept_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <collection property="exprList" ofType="org.example.pojo.EmpExpr">
            <id property="id" column="ee_id"/>
            <result property="empId" column="ee_emp_id"/>
            <result property="begin" column="ee_begin"/>
            <result property="end" column="ee_end"/>
            <result property="job" column="ee_job"/>
            <result property="company" column="ee_company"/>
        </collection>
    </resultMap>

    <select id="findById" resultMap="empResultMap">
        select
            emp.*,
            emp_expr.id ee_id,
            emp_expr.emp_id ee_emp_id,
            emp_expr.begin ee_begin,
            emp_expr.end ee_end,
            emp_expr.job ee_job,
            emp_expr.company ee_company
        from emp left join emp_expr on emp.id = emp_expr.emp_id
        where emp.id = #{id}
    </select>

    <!--统计职位人数-->
    <select id="countEmpByJob" resultType="java.util.Map">
        select
            (case when job = 1 then '班主任'
                  when job = 2 then '讲师'
                  when job = 3 then '学工主管'
                  when job = 4 then '教研主任'
                  when job = 5 then '咨询师'
                  else '其他' end) pos,
            count(*) num
        from emp
        group by job order by num
    </select>

    <select id="countEmpByGender" resultType="java.util.Map">
        select
            if(gender = 1, '男', '女') gen,
            count(*) num
        from emp
        group by gender
    </select>

    <update id="updateById">
        update emp
        <!--set标签：动态生成set标签，删除多余的逗号-->
        <set>
            <if test="username != null and username != '' " >
                username = #{username},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="phone != null and job != ''">
                phone = #{phone},
            </if>
            <if test="job != null">
                job = #{job},
            </if>
            <if test="salary != null">
                salary = #{salary},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="entryDate != null">
                entry_date = #{entryDate},
            </if>
            <if test="deptId != null">
                dept_id = #{deptId},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>